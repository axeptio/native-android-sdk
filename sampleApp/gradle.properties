org.gradle.jvmargs=-Xmx2048m -Dfile.encoding=UTF-8
org.gradle.parallel=true
org.gradle.caching=true

android.useAndroidX=true
kotlin.code.style=official

# Version of io.axeptio:sdk this example depends on. The publish workflow pins
# this to the released version; change it to try a different one.
axeptioVersion=1.0.1
